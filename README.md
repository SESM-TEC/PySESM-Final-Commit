# PySESM
SESM is an approach to data representation that utilizes a shared parametric dictionary to create surrogate models for data sub-blocks. This enables efficient knowledge transfer between similar contexts, enhancing model flexibility and performance.

To install PySESM:

> python setup.py install
